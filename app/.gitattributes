# Normalize line endings so generated files (tools/build_app_data.mjs output) stay
# byte-stable across platforms — a future "regenerate + git diff" CI guard must not
# fail on CRLF/LF. Mark binary assets so git never touches them.
* text=auto eol=lf
*.mp3 binary
